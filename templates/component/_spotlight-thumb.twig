
    <div class="col-md-4 col-sm-4 col-xs-12">
        <div class="spotlight-item-thumb">
            <div class="spotlight-item-thumb-img">
                <a href="{{ path('index_article', {'category': article.category.slug, 'slug': article.slug, 'id': article.id}) }}">
                    <img alt="" src="{{ asset('images/product/' ~ article.featuredImage) }}" />
                </a>
                <a href=" {{ path('index_category', {'category': article.category.slug }) }}" class="cate-tag">{{ article.category.slug | lower}}</a>
            </div>
            <h3><a href="{{ path('index_article', {'category': article.category.slug, 'slug': article.slug, 'id': article.id}) }}">{{ article.title }}</a></h3>
            <div class="meta-post">
                <a href="#">
                    {{ article.author.firstname ~' '~article.author.lastname }}
                </a>
                <em></em>
                <span>
					{{ article.datecreation | date("d/m/Y") }}
                </span>
            </div>
        </div>
    </div>